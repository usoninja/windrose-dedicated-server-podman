# Container identity and image
# Keep HOSTNAME=localhost for Windrose ICE compatibility. Change only if you know DNS resolution works.
CONTAINER_NAME=windrose
HOSTNAME=localhost
IMAGE_NAME=localhost/windrose-ds:stable

# Host permissions
PUID=1000
PGID=1000

# Steam / game
STEAM_LOGIN=anonymous
STEAM_PASS=
WINDROSE_APP_ID=4129620
UPDATE_ON_START=true
UPDATE_VERIFY_TIMEOUT=120
GENERATE_SETTINGS=true

# ServerDescription.json patching
INVITE_CODE=
SERVER_NAME=My Windrose Server
SERVER_NOTE=Friendly co-op server
SERVER_PASSWORD=
MAX_PLAYERS=4
P2P_PROXY_ADDRESS=127.0.0.1

# Direct connection (alternative to invite code, requires port forwarding)
# Set USE_DIRECT_CONNECTION=true and expose DIRECT_CONNECTION_SERVER_PORT on the host.
USE_DIRECT_CONNECTION=false
DIRECT_CONNECTION_SERVER_PORT=7777
DIRECT_CONNECTION_PROXY_ADDRESS=0.0.0.0
# Region for the connection service: SEA, CIS, EU. Leave empty for auto-detect.
USER_SELECTED_REGION=
# auto: prefer Gotify then Discord, both: send to both backends
NOTIFY_PROVIDER=auto
DISCORD_WEBHOOK_URL=
GOTIFY_URL=
GOTIFY_TOKEN=
GOTIFY_PRIORITY=5

# Backups (used by the helper script on the host)
BACKUP_DIR=backups
BACKUP_RETENTION_DAYS=7
BACKUP_SCOPE=full
BACKUP_FORMAT=tar.gz
BACKUP_NOTIFY_SUCCESS=false
BACKUP_NOTIFY_FAIL=true
BACKUP_DISCORD_UPLOAD=false

# Networking
PORT=7777
QUERYPORT=7778
MULTIHOME=0.0.0.0
